# Copy to backend/.env and fill in OPENAI_API_KEY. Everything else has a
# working default for a standard local run.

# ---------------------------------------------------------------------------
# backend/.env  — read by the Agno process (python-dotenv)
# ---------------------------------------------------------------------------

# REQUIRED. The model key. Only the Agno process ever holds it; it never
# reaches the browser and never reaches the Copilot Runtime.
OPENAI_API_KEY=sk-...

# Seconds the getWeather tool sleeps before returning. Default: 1.5
# Without it the tool returns in ~23ms — about one frame — so a tool renderer's
# "in-progress" and "executing" states never get painted and only the completed
# card is ever visible. Set to 0 for a tool that returns instantly.
WEATHER_TOOL_DELAY=0

# ---------------------------------------------------------------------------
# Copilot Runtime  — frontend/server.ts, read from the shell that starts it
# ---------------------------------------------------------------------------

# Where the runtime finds the Agno agent's AG-UI endpoint.
# Default: http://localhost:8000/agui
# Use 127.0.0.1 instead of localhost if DNS resolves to IPv6 while the agent
# binds IPv4.
AGNO_AGENT_URL=http://localhost:8000/agui

# Port the Copilot Runtime listens on. Default: 8200.
# The Angular app's runtimeUrl in src/app/app.config.ts is hardcoded to 8200,
# so change both together.
PORT=8200

# Opt out of CopilotKit's anonymous runtime telemetry.
# COPILOTKIT_TELEMETRY_DISABLED=true
